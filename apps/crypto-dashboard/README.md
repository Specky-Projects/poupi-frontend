# Crypto Dashboard

Next.js app para o dashboard pessoal de cripto.

A interface antiga do projeto `bot-crypto-coin` foi preservada em `legacy/` apenas como referencia visual/funcional. Codigo novo deve ser implementado em `src/app` e consumir o Data Core via `@poupi-frontend/api-client`.
